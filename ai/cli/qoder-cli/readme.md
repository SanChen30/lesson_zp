# 🚀 qoder-cli 全方位实战指南 (Windows 终极版)

> **核心定位**：基于阿里 Qwen 编程大模型构建的 AI Agent 命令行框架。
> **一句话描述**：它不只是一个代码生成器，而是一个能看懂文档、能操作浏览器的“数字员工”。

## 1. 核心理念：为什么我们需要命令行 Agent？

在 Trae/Cursor 等 AI 编辑器（IDE）盛行的今天，`qoder-cli` 代表了另一种开发哲学——**“双引擎开发模式”**。

| 维度 | AI IDE (Cursor/Trae) | AI CLI (qoder-cli) |
| --- | --- | --- |
| **交互方式** | 图形界面，鼠标点击 | 纯文本指令，脚本自动化 |
| **擅长场景** | 深度上下文理解、多文件重构、代码补全 | **快速原型搭建、任务自动化、脚本编写、CI/CD流水线集成** |
| **优势** | 沉浸式体验，适合精细操作 | **速度极快**、灵活性高、可脱离鼠标全键盘操作 |

**结论**：未来的超级开发者是 **IDE + CLI** 双修。IDE 负责写核心业务，CLI 负责干脏活累活（生成脚手架、写测试、爬文档）。

## 2. 快速上手

### 安装核心框架

```bash
npm i -g @qoder-ai/qodercli

```

### 初始化项目 (`/init`)

进入任意空文件夹，运行：

```bash
qodercli

```

在交互界面输入 `/init`。

* **产物**：生成 `AGENTS.md` 文件。
* **作用**：这是 AI 的“人设剧本”。你可以在这里定义你的 AI 助手是“资深 React 专家”还是“Python 数据分析师”。LLM 会根据这个文件调整代码风格。

## 3. MCP 协议：给 AI 装上“机械臂”

**MCP (Model Context Protocol)** 是让大模型连接外部世界的通用标准。没有 MCP，AI 只是一个聊天机器人；有了 MCP，AI 变成了能干活的 Agent。

### 必备工具介绍

#### 🛠️ 工具 A: Context7 (知识补丁)

* **痛点**：大模型训练数据有截止日期（比如不知道最新的 Next.js 14 写法），容易产生“幻觉”或写出过时代码。
* **解法**：Context7 是一个实时文档抓取工具。
* **工作流**：
1. 你请求生成代码。
2. Context7 自动去抓取官方最新的文档/API。
3. 将最新文档作为“上下文”喂给 Qwen 模型。
4. **结果**：AI 写出了它从未学过的最新版本代码。



#### 🛠️ 工具 B: Playwright (数字之眼)

* **痛点**：AI 无法看见网页实际运行的样子，也无法帮你在网页上点点点。
* **解法**：Playwright 是浏览器自动化工具。
* **工作流**：
* 你可以让 AI：“去 GitHub 帮我看看这个库最新的 Issue 是什么。”
* 你可以让 AI：“帮我给这个网页写一个端到端测试脚本。”



## 4. Windows 环境配置实战 (✅ 验证通过)

由于 Windows 命令行对参数解析的特殊性，直接使用 `npx -y` 容易导致连接失败。我们采用**“先全局安装，后直接调用”**的策略，这是最稳健的方法。

### 第一步：清理旧配置 (必做)

为了防止之前的错误配置干扰，先执行清理：

```powershell
qodercli mcp remove playwright
qodercli mcp remove context7

```

### 第二步：全局安装 MCP 工具

这一步是一次性的。我们在系统层安装工具，这样后续调用就不需要输入 `y` 确认，也就不需要传递复杂的参数了。

```powershell
npm install -g @playwright/mcp @upstash/context7-mcp

```

*(请等待安装进度条完成)*

### 第三步：添加服务到 qoder-cli

使用 `npx.cmd` (Windows 专用指令) 直接调用，**不需要加引号**，也**不需要加 -y**。

```powershell
# 添加 Context7 (文档助手)
qodercli mcp add context7 npx.cmd @upstash/context7-mcp

# 添加 Playwright (浏览器助手)
qodercli mcp add playwright npx.cmd @playwright/mcp

```

### 第四步：验证状态

```powershell
qodercli mcp list

```

* ✅ **成功标志**：状态显示 `Connected`。
* ❌ **失败标志**：状态显示 `Disconnected` 或 `Error`。

---

## 5. 常用指令

| 动作 | 命令 | 说明 |
| --- | --- | --- |
| **查看列表** | `qodercli mcp list` | 检查所有工具连接状态 |
| **查看详情** | `qodercli mcp get [name]` | 查看具体工具的配置参数 |
| **删除工具** | `qodercli mcp remove [name]` | 卸载指定工具 |

## 6. 使用场景示例

配置好 Context7 和 Playwright 后，你的 `qoder-cli` 将拥有超能力：

**场景一：使用最新技术栈编程 (Context7 生效)**

> User: "用最新的 Tailwind CSS v4 语法帮我写一个卡片组件。"
> **Agent 内部思考**: *模型原数据里没有 Tailwind v4，调用 Context7 获取文档... 学习新语法... 生成代码。*

**场景二：自动化网页测试 (Playwright 生效)**

> User: "帮我测试一下我的本地 localhost:3000 页面，确保登录按钮可以点击。"
> **Agent 内部思考**: *调用 Playwright 启动无头浏览器... 访问页面... 模拟点击... 返回测试结果。*

### 总结

`qoder-cli` + `MCP` = **会自我学习、能操作浏览器的全能编程助手**。


## 实践

- 在一个空文件夹中进行操作
1. 在终端用`qodercli`命令打开**qoder命令行**
1. 登录输入：`/login`
2. 输入：”如何配置vue-router，使用最新版本，use context7，并用中文回答“

- 得到回答：
“
我来为您提供 Vue Router 最新版本的配置指南：

  Vue Router 4 配置指南
  Vue Router 4（适用于 Vue 3 的最新版本）相比 Vue Router 3 使用了不同的 API。以下是设置方法：

  1. 安装
  首先安装 Vue Router 4：

  npm install vue-router@4

  2. 设置路由器
  创建一个 router/index.js 文件（或 router/index.ts 如果使用 TypeScript）：

  import { createRouter, createWebHistory } from 'vue-router'

  // 定义路由组件
  const Home = () => import('../views/Home.vue')
  const About = () => import('../views/About.vue')
  const Contact = () => import('../views/Contact.vue')

  // 定义路由
  const routes = [
    {
      path: '/',
      name: 'Home',
      component: Home
    },
    {
      path: '/about',
      name: 'About',
      component: About
    },
    {
      path: '/contact',
      name: 'Contact',
      component: Contact
    }
  ]

  // 创建路由器实例
  const router = createRouter({
    history: createWebHistory(),
    routes
  })

  export default router

  3. 在应用中安装路由器
  在您的 main.js 文件中：

  import { createApp } from 'vue'
  import App from './App.vue'
  import router from './router'

  const app = createApp(App)

  // 安装路由器                                                                                                                                      
  app.use(router)

  app.mount('#app')

  4. 在组件中使用路由器
  在您的 App.vue 或其他组件中，使用 router-view 和 router-link：

  <template>
    <div id="app">
      <nav>
        <router-link to="/">首页</router-link>
        <router-link to="/about">关于</router-link>
        <router-link to="/contact">联系</router-link>
      </nav>
      <main>
        <router-view></router-view>                                                                                                                  
      </main>
    </div>
  </template>

  <script>
  export default {
    name: 'App'
  }
  </script>

  5. 额外配置选项
  您还可以配置其他选项：

  import { createRouter, createWebHistory } from 'vue-router'

  const routes = [
    // 您的路由
  ]

  const router = createRouter({
    history: createWebHistory(process.env.BASE_URL),
    routes,
    // 可选：滚动行为
    scrollBehavior(to, from, savedPosition) {
      if (savedPosition) {
        return savedPosition
      } else {
        return { top: 0 }
      }
    }
  })

  export default router

  与 Vue Router 3 的主要区别：
  1. 导入方式：使用命名导入 (createRouter, createWebHistory)
  2. 历史模式：使用 createWebHistory() 明确指定 HTML5 历史模式
  3. 安装方式：使用 app.use(router) 而不是将路由器传递给 Vue 构造函数
  4. 懒加载：使用动态导入 () => import('./Component.vue')

  这种配置遵循最新的 Vue Router 4 标准，并使用与 Vue 3 兼容的组合式 API 方法。
”