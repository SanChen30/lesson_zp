# useRef hooks 函数

- useEffect + []

等同于 vue onMounted 生命周期

- 怎么找到 input 元素

ref + useRef 可变对象

- 与 useState 比较

相同点：都是存储可变对象的容器

useState 可变带来状态

useRef 可变对象的存储

useState 是响应式状态，useRef 不是响应式的

## useRef 应用场景

- dom节点的引用

- 可变对象的存储

- 创建的是可持久化的引用对象


