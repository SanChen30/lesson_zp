# 受控组件和非受控组件

- 怎么拿到表单的值？

表单元素

value 双向的，需要 value, 又能输入改变 value

- 状态 单向数据绑定，控制表单元素的值

input value = {value}

数据状态驱动页面，状态不能出问题的，受控组件

收集用户的输入，onChange 改变 value 状态

- useRef + ref 持久化的保存一个可变对象，可以是第一个输入框的值的容器

用 .current.value 拿到输入框的值

## 区别

非受控组件适合一次性读取，性能敏感，文件上传等
受控组件适合表单校验、联动、实时展示、表单的即时验证