# stack

1. 栈是什么
    栈是一种后进先出（LIFO）的线性数据结构。

2. 栈的ADT
    栈的抽象数据类型（ADT）定义了栈的操作和行为。
    
    栈的ADT包括以下操作：
    - push：将元素压入栈顶。
    - pop：从栈顶弹出元素。
    - peek：获取栈顶元素，不弹出。
    - isEmpty：检查栈是否为空。
    - size：获取栈的元素数量。